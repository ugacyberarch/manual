---
label: navigation-header
hide_body: false
fields:
- name: block
  type: text
  config:
    required: false
  label: block
  hidden: true
  default: header-1
- name: logo
  type: file
  config:
    maxSize: 64
  label: Logo
- name: navigation
  type: field_group_list
  fields:
  - name: link
    type: text
    config:
      required: false
    label: link
  - name: link_text
    type: text
    config:
      required: false
    label: Link text
  config:
    min: 
    max: 
    labelField: link_text
  label: Navigation
