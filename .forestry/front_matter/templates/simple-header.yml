---
label: simple-header
hide_body: false
fields:
- name: block
  type: text
  config:
    required: false
  hidden: true
  label: block
  default: header-3
- name: logo
  type: file
  config:
    maxSize: 64
  label: Logo
